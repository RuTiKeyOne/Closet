import 'package:closet/presentation/open_box/open_box_view.dart';
import 'package:flutter/material.dart';

class OpenBoxScreen extends StatelessWidget {
  const OpenBoxScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const OpenBoxView();
  }
}
